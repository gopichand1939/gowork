import Link from 'next/link';
import {ArrowUpRight} from 'lucide-react';
import type {Project} from '@/lib/projects';
import {ProjectVisual} from './project-visual';
export function ProjectCard({project,index=0,featured=false}:{project:Project;index?:number;featured?:boolean}){return <article className={`project-card ${featured?'featured-project':''} ${project.theme==='kitchen'?'kitchen-project':''}`}><Link href={`/work/${project.slug}`} className="project-image-link" aria-label={`View ${project.name} case study`}><ProjectVisual project={project} featured={featured}/><span className="image-hover-label">Explore the case study <ArrowUpRight size={18}/></span></Link><div className="project-info"><div className="project-heading"><div><p className="project-category"><span>0{index+1}</span> {project.category}</p><h3><Link href={`/work/${project.slug}`}>{project.name}</Link></h3></div><Link href={`/work/${project.slug}`} className="circle-link" aria-label={`${project.name} case study`}><ArrowUpRight size={23}/></Link></div><p className="project-summary">{project.summary}</p><div className="project-tags">{project.tags.map(x=><span key={x}>{x}</span>)}</div><div className="project-role"><span>{project.role}</span><Link href={`/work/${project.slug}`}>View case study <ArrowUpRight size={14}/></Link></div></div></article>}
